package algorithmTask;

public class BracketCount {
    public static boolean bracketAngleCounter(String input){
        int leftCounter = 0;
        int rightCounter = 0;
        if (input.length() % 2 != 0){
            throw new IllegalStateException("invalid bracket");
        }
        for (int i= 0; i < input.length(); i++){
        }

        return false;
    }
}
